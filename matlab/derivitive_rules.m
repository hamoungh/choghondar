% y       ?y/?x
% --------------
% Ax      A'
% x'A     A
% x'x     2x
% x'Ax    Ax+A'x
%
% d(?X) = ? dX
% d(X + Y) = dX + dY
% d(XY) = (dX)Y + X(dY)
%
% -- (X^-1) X = I =>
% -- d(X^-1)X + (X^-1)dX = 0 =>
% d(X^-1) = -(X^-1)dX(X^-1)
% d(X')/dX=I
% d(X'AXX')/dX=(A+A')XX'+X'AXI
% d(X^-1)/dz=-(X^-1)(dX/dz)(X^-1)
% d(AXB)/dz=A(dX/dz)B


% see http://www.psi.toronto.edu/matrix/calculus.html
% or matrix identities by sam roweis
% for more on Matrix Calculus
